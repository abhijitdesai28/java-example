package types;

/**
 * Member Inner Class:
 * this is the most common type of inner class
 * it is defined at the member level of a class and can access all members(static & non-static) of the outer class
 * to create an instance of a member inner class,you first nedd to create an instance of the outer class
 */

public class MemberInnerClass {

    //body of outer nested class

    class Innerclass{

        ////body of inner nested class
    }
}
